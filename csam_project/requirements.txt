# CSAM - Cognitive Sparse Access Memory
# Core dependencies

# Vector search
# hnswlib  # Optional: requires C++ build tools on Windows, using FAISS instead

# Embeddings
sentence-transformers

# Graph storage
networkx

# Database
sqlalchemy

# HTTP API (async-ready)
fastapi
uvicorn[standard]  # ASGI server for FastAPI

# LLM client
requests

# Scientific computing
numpy
scipy

# Evaluation
scikit-learn
pandas
matplotlib

# Testing
pytest
pytest-asyncio

# ============================================
# GPU Acceleration & Async (New)
# ============================================
# Deep learning for GPU-accelerated embeddings
torch
# Note: For GPU support, install with CUDA:
#   pip install torch --index-url https://download.pytorch.org/whl/cu118  (CUDA 11.8)
#   pip install torch --index-url https://download.pytorch.org/whl/cu121  (CUDA 12.1)
# The same 'torch' package works for both CPU and GPU

# FAISS for vector search (CPU version for Python 3.11+ compatibility)
# GPU version requires conda: conda install -c pytorch faiss-gpu
faiss-cpu

# Async HTTP client for concurrent LLM requests
aiohttp

# Utilities
python-dotenv
tqdm
